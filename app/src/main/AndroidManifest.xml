<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.inductionapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/jssate"
        android:label="@string/app_name"
        android:roundIcon="@drawable/jssate"
        android:supportsRtl="true"
        android:theme="@style/Theme.InductionApp">
        <activity android:name=".AboutCollegeActivity"></activity>
        <activity android:name=".AddDetailsActivity" />
        <activity android:name=".AdminDetailsActivity" />
        <activity android:name=".AddActivity" />
        <activity android:name=".DetailsActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>